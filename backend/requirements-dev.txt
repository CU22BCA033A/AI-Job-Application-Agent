-r requirements.txt

# Test tooling only — uvicorn moved to requirements.txt since it's now a
# genuine runtime dependency (Render runs this app directly via uvicorn).
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0
