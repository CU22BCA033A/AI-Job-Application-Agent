# Runtime dependencies — installed for local `uvicorn` runs AND for the
# Vercel Python serverless function. Keep this lean: anything added here
# gets bundled into the deployed function, so dev/test-only tools belong
# in requirements-dev.txt instead.
fastapi==0.115.0
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
anthropic==0.39.0
python-dotenv==1.0.1
python-multipart==0.0.12
pypdf==5.0.1

# Postgres driver — needed in production (serverless has no persistent
# local disk, so SQLite only works for local dev). Harmless to install
# even when running locally against SQLite.
psycopg[binary]==3.2.3
